# Copy this file to .env.local for local development.
# The project already contains a remote .env file, so do not overwrite it.

POSTGRES_USER=kbo
POSTGRES_PASSWORD=kbo
POSTGRES_DB=kbo
POSTGRES_HOST=localhost
POSTGRES_PORT=5434
DATABASE_URL=postgresql+psycopg://kbo:kbo@localhost:5434/kbo
